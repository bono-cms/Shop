
<?php defined('KRYSTAL') or die; ?>
<h2><small># <?php echo $id; ?></small></h2>

<div class="row">
    
    <div class="col-lg-8">
        <div class="table-responsive">
            <table class="table table-hover table-bordered table-striped">
                <thead>
                    <tr>
                        <th class="text-center"><?php $this->show('#'); ?></th>
                        <th class="text-center"><?php $this->show('Product'); ?></th>
                        <th class="text-center"><?php $this->show('Price'); ?></th>
                        <th class="text-center"><?php $this->show('Sub total price'); ?></th>
                        <th class="text-center"><?php $this->show('Quantity'); ?></th>
                    </tr>
                </thead>

                <?php if (!empty($details)): ?>
                <tbody>
                    <?php foreach ($details as $detail): ?>

                    <tr class="<?php echo !$detail['exists'] ? 'danger' : null; ?>" title="<?php !$detail['exists'] ? $this->show('This product is no longer available') : null; ?>">
                        <td class="text-center"><?php echo $detail['product_id']; ?></td>
                        <td>
                            <?php if ($detail['exists']): ?>
                            <a target="_blank" href="<?php echo $detail['url']; ?>"><?php echo $detail['name']; ?></a>

                            <?php else: ?>
                            <?php echo $detail['name']; ?>

                            <?php endif; ?>
                        </td>
                        <td class="text-center"><?php echo number_format($detail['price']); ?> <?php echo $currency; ?></td>
                        <td class="text-center"><?php echo number_format($detail['sub_total_price']); ?> <?php echo $currency; ?></td>
                        <td class="text-center"><?php echo $detail['qty']; ?></td>
                    </tr>
                    <?php endforeach; ?>
                </tbody>
                <?php endif; ?>
            </table>
        </div>
    </div>

    <div class="col-lg-4">
        <h4><i class="glyphicon glyphicon-th-list"></i> <?php $this->show('Order details'); ?></h4>

        <table class="table table-bordered table-hover">
            <tbody>
                <tr>
                    <td><?php $this->show('Order ID'); ?></td>
                    <td><?php echo $id; ?></td>
                </tr>

                <tr>
                    <td><?php $this->show('Total quantity'); ?></td>
                    <td><?php echo $summary['totalQty']; ?></td>
                </tr>

                <tr>
                    <td><?php $this->show('Total price'); ?></td>
                    <td><?php echo number_format($summary['totalPrice']); ?> <?php echo $currency; ?></td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

